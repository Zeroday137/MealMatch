# MealMatch Project 
to run the Expo server in VS Code 

## run 
install node.js 
-needed for Expo to run 
npm install 
-Downloads the project dependencies.
npx expo start
-Starts the app development server.

